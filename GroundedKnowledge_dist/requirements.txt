# Web framework & server
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4

# PDF export
reportlab==5.0.1

# RAG retrieval (used by the GroundedKnowledge-content skill's query.py)
fastembed==0.8.0
numpy==2.5.2

# macOS menu bar tray icon (macOS only — skip on Linux/Windows)
rumps==0.4.0
